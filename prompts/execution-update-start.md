Switch to execution-update mode.
Read the active mode files, `tasks.md` for the target slice, and the current `actual-progress.puml` files.
Update execution facts without changing baseline planning estimates unless the user explicitly switches back to planning.
