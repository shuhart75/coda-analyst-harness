# Start: release-finalization

1. Read `AGENTS.md`, `.workflow/llm-contract.md`, `.workflow/active-mode.md`, `.workflow/modes/release-finalization.md`.
2. Read `baseline/current/` to understand the current deployed state.
3. Read the relevant `features/*/domain-impact.md`, delivered requirements and execution facts.
4. Write final release artifacts under `releases/` first, then update `baseline/current/` and archive the previous baseline under `baseline/versions/`.
