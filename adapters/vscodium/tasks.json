{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Workflow: Planning",
      "type": "shell",
      "command": "bash ${workspaceFolder}/.workflow/tools/switch-mode.sh ${workspaceFolder} planning"
    },
    {
      "label": "Workflow: Requirements",
      "type": "shell",
      "command": "bash ${workspaceFolder}/.workflow/tools/switch-mode.sh ${workspaceFolder} requirements"
    },
    {
      "label": "Workflow: Scope Prototype",
      "type": "shell",
      "command": "bash ${workspaceFolder}/.workflow/tools/switch-mode.sh ${workspaceFolder} scope-prototype"
    },
    {
      "label": "Workflow: Delivery Prototype",
      "type": "shell",
      "command": "bash ${workspaceFolder}/.workflow/tools/switch-mode.sh ${workspaceFolder} delivery-prototype"
    },
    {
      "label": "Workflow: Execution Update",
      "type": "shell",
      "command": "bash ${workspaceFolder}/.workflow/tools/switch-mode.sh ${workspaceFolder} execution-update"
    },
    {
      "label": "Workflow: Release Finalization",
      "type": "shell",
      "command": "bash ${workspaceFolder}/.workflow/tools/switch-mode.sh ${workspaceFolder} release-finalization"
    },
    {
      "label": "Workflow: Validate Structure",
      "type": "shell",
      "command": "python3 ${workspaceFolder}/.workflow/tools/validate-structure.py ${workspaceFolder}"
    },
    {
      "label": "Workflow: Validate Links",
      "type": "shell",
      "command": "python3 ${workspaceFolder}/.workflow/tools/validate-links.py ${workspaceFolder}"
    },
    {
      "label": "Workflow: Doctor",
      "type": "shell",
      "command": "python3 ${workspaceFolder}/.workflow/tools/harnessctl.py doctor ${workspaceFolder}"
    },
    {
      "label": "Workflow: Session Brief",
      "type": "shell",
      "command": "python3 ${workspaceFolder}/.workflow/tools/harnessctl.py session-brief ${workspaceFolder}"
    },
    {
      "label": "Workflow: Generate Draft Plan",
      "type": "shell",
      "command": "python3 ${workspaceFolder}/.workflow/tools/sync-planning-gantt.py ${workspaceFolder} ${input:quarterId}"
    }
  ],
  "inputs": [
    {
      "id": "quarterId",
      "type": "promptString",
      "description": "Quarter id",
      "default": "2026-Q3"
    }
  ]
}
